"use client";
import { useEffect,useState } from "react";
import { supabase } from "@/lib/supabase";
const ADMIN_EMAIL="milagroslove.1693@gmail.com";
type Order={id:string;user_id:string;status:string;total:number;payment_method:string|null;created_at:string};
export default function AdminPage(){const[orders,setOrders]=useState<Order[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const load=async()=>{if(!supabase){setError("Supabase no está configurado.");setLoading(false);return;}const{data:s}=await supabase.auth.getSession();const user=s.session?.user;if(!user){window.location.replace("/login?next=%2Fadmin");return;}if((user.email||"").toLowerCase()!==ADMIN_EMAIL){window.location.replace("/catalogo");return;}const{data,error}=await supabase.from("orders").select("id,user_id,status,total,payment_method,created_at").order("created_at",{ascending:false});if(error)setError(`${error.code||"Error"}: ${error.message}`);else setOrders((data||[]) as Order[]);setLoading(false);};useEffect(()=>{load();},[]);const changeStatus=async(id:string,status:string)=>{if(!supabase)return;setError("");const{error}=await supabase.from("orders").update({status}).eq("id",id);if(error){setError(`${error.code||"Error"}: ${error.message}`);return;}await load();};return <main className="mx-auto min-h-[75vh] max-w-5xl px-4 py-14"><p className="text-sm font-bold uppercase tracking-wider text-[#e3b64f]">Administración</p><h1 className="mt-2 text-4xl font-black">Pedidos</h1><p className="mt-3 text-white/50">Confirma el pago después de verificar el voucher recibido por WhatsApp.</p>{loading&&<p className="mt-10 text-white/50">Cargando pedidos...</p>}{error&&<p className="mt-6 rounded-xl border border-red-400/20 bg-red-400/10 p-4 text-red-200">{error}</p>}<div className="mt-8 space-y-4">{orders.map(o=><article key={o.id} className="rounded-3xl border border-white/10 bg-white/[0.04] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-mono text-sm text-[#e3b64f]">{o.id.slice(0,8).toUpperCase()}</p><p className="mt-1 text-xs text-white/40">{new Date(o.created_at).toLocaleString("es-PE")}</p></div><p className="text-xl font-black">S/ {Number(o.total).toFixed(2)}</p></div><p className="mt-4 text-sm">Estado: <b>{o.status}</b></p><p className="mt-1 text-xs text-white/40">Método: {o.payment_method?.toUpperCase()||"—"}</p><div className="mt-5 grid gap-2 sm:grid-cols-3"><button onClick={()=>changeStatus(o.id,"paid")} className="rounded-xl bg-emerald-500 px-4 py-3 font-black text-white">Confirmar pago</button><button onClick={()=>changeStatus(o.id,"processing")} className="rounded-xl bg-amber-400 px-4 py-3 font-black text-black">Procesando</button><button onClick={()=>changeStatus(o.id,"delivered")} className="rounded-xl bg-[#e3b64f] px-4 py-3 font-black text-black">Marcar entregado</button></div></article>)}</div></main>;}